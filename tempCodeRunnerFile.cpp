if (score > bestScore) {
                                            bestScore = score;
                                            }
                    bestScoreText.setString(std::to_string(bestScore));
                    updateMedal(score, medalTexture, medalSprite,bgSize);